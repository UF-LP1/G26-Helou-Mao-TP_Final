#include "funciones.h"

void readpatient(string name, list<cMedico*> listaMed) //SI O SI EN UN TRY CATCH
{
	//if (&listaMed == nullptr)
	//	throw "puntero a nulo";
	//fstream file;
	//file.open(name, ios::in);
	//if (!(file.is_open()))
	//	throw "error para abrir archivo";
	//char coma;
	//cMedico* headers;
	//file >> headers-> >> coma >> headers.name >> coma >> headers.surname >> coma >> headers.sex >> coma >> headers.birth.day;
	////COMO HAGO PARA LEVANTAR EL AÑO COMO ME LO DAN??
	//while (file)
	//{
	//	patient aux;
	//	file >> aux.ID >> coma >> aux.name >> coma >> aux.surname >> coma >> aux.sex >> coma >> aux.birth.day;
	//	bool added = addPatient(list_patient, Npatient, aux);
	//	if (added == false)
	//		return false;
	//}
	//file.close();
	//return true;
}